print('..Building App')

env = Environment(
    CC = 'g++',
    CCFLAGS = '-O2',
    SCONS_CXX_STANDARD='c++11'
)

env.AppendUnique(CPPPATH = ['.', '../BD_FACADE', '../graphic_library_facade', '../../libraries/SFML-2.5.1/include', '../../libraries/boost_1_72_0'])

env.Program('hello', Glob('*.cpp', '*h'))