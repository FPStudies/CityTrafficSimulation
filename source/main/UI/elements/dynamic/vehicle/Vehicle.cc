/*
 * Vehicle.cc
 *
 *      Author: Przybysz Filip, Kordowski Mateusz
 */

#include "dynamic/vehicle/Vehicle.h"

using namespace Elements::Vehicle;

Vehicle::Vehicle(sf::Window& window, const Texture_ptr& texture) : DynamicElement(window, texture) {};